[project]
name = "coldstation"
version = "0.1.0"
description = "Cold-station load & equipment-parameter forecasting pipeline (competition internal)"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.2",
    "numpy>=1.26",
    "scikit-learn>=1.4",
    "openpyxl>=3.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
