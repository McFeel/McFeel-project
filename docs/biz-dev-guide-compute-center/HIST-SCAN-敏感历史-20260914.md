# PR #92 敏感历史扫描（只读，2026-09-14）

扫描范围：`origin/main..HEAD`（分支 `cursor/biz-dev-guide-compute-center-9684`，当前 `ec07409`）。  
方法：`git fetch` 后对每提交 `ls-tree` + blob 关键词分类；`git log -S` 交叉核对。  
本报告只记提交 SHA、路径、敏感类别、短 blob SHA、可达性。不写命中原文、会话 ID、Notion 链接或正文片段。

## 1. 分支提交列表（相对 `main`）

基准：`origin/main` = `76aff6f`。线性 6 提交，无 merge。

| 顺序 | SHA | 时间 (UTC) | 说明（仅标题，不展开） |
| --- | --- | --- | --- |
| 1 | `4a2c933` | 00:38 | 初稿骨架 + 对标摘要入仓 |
| 2 | `6f96f36` | 00:41 | 对标结构对齐 |
| 3 | `e896cef` | 00:44 | 补漏材料入骨架 |
| 4 | `6b46b6e` | 00:48 | 追加专责记忆 / 经验日志 |
| 5 | `1be59c7` | 00:50 | 经验日志改格式（引入会话 ID / Notion 卡） |
| 6 | `ec07409` | 06:08 | 公开仓脱敏：删记忆与经验日志，改骨架 |

当前树（`ec07409`）已删除：

- `docs/specialist-memory/算力专责.md`
- `docs/biz-dev-guide-compute-center/EXPERIENCE.md`

当前 `docs/` 仅余骨架与对标摘要；对上述关键词做 `git grep`：**HEAD 树无命中**。

## 2. 命中表

「当前树」= 该路径是否仍在 `HEAD` 工作树。  
「分支可达」= 对象仍挂在本分支历史（`git rev-list --objects HEAD` 可取到），公开仓 / PR 页面仍能读到。

### 2.1 已删除路径（高风险：会话 / Notion / 专责记忆）

| commit | path | 敏感类别 | blob（短） | 当前树 | 分支可达 |
| --- | --- | --- | --- | --- | --- |
| `6b46b6e` | `docs/specialist-memory/算力专责.md` | 专责记忆路径；会话 ID；备忘录 | `5714c6a5560a` | 否 | 是 |
| `6b46b6e` | `docs/biz-dev-guide-compute-center/EXPERIENCE.md` | EXPERIENCE 路径；会话 ID | `8afb516bf206` | 否 | 是 |
| `1be59c7` | `docs/specialist-memory/算力专责.md` | 专责记忆路径；会话 ID；Notion URL；备忘录；Notion 经验 | `e2fdcf80753d` | 否 | 是 |
| `1be59c7` | `docs/biz-dev-guide-compute-center/EXPERIENCE.md` | EXPERIENCE 路径；会话 ID；Notion URL；Notion 经验 | `c3ec22ecc018` | 否 | 是 |

上述 4 个 blob **不在当前树**，但只要本分支 / PR 仍指向含这些父提交的历史，对象就仍公开可达。

### 2.2 历史骨架（已不在当前树 blob，路径仍在）

当前 HEAD 骨架 blob = `e2ae802e9b99`，精确关键词已净。历史版本仍可达：

| commit | path | 敏感类别 | blob（短） | 当前树路径 | 同 blob 在 HEAD | 分支可达 |
| --- | --- | --- | --- | --- | --- | --- |
| `4a2c933` | `docs/biz-dev-guide-compute-center/算力中心业务开发指引-骨架-20260914.md` | 备忘录；专责记忆 | `4b1edccd682f` | 是 | 否 | 是 |
| `6f96f36` | 同上 | 备忘录；专责记忆 | `c1aa74bd7d1b` | 是 | 否 | 是 |
| `e896cef` / `6b46b6e` / `1be59c7` | 同上 | 项目代号；地名/当事方；备忘录；专责记忆 | `d19df80111c8` | 是 | 否 | 是 |

`d19df80111c8` 为脱敏前骨架同一 blob，跨 3 个提交复用。

### 2.3 提交说明本身（元数据）

| commit | 敏感类别 | 说明 |
| --- | --- | --- |
| `e896cef` | 项目代号 | 标题含内部材料代号 |
| `6b46b6e` | 专责记忆；EXPERIENCE 路径 | 标题点名双写路径 |
| `1be59c7` | 会话 ID / Notion 经验（描述性） | 标题声明引入该类对象 |

提交说明无法靠删文件消除，需改写历史或弃用本分支才能从默认浏览路径拿掉。

### 2.4 扫描但未在 blob 正文命中

| 关键词 | 结果 |
| --- | --- |
| `里水` | 无 |
| `超算芯` | 无 |
| `Cloud 会话` | 无 |
| `notion.so` | 无（仅见 `app.notion.com` 类） |
| `docs/biz-dev-guide-compute-center/对标摘要.md` | 精确关键词无命中（各历史 blob 亦不含上表类别） |

## 3. 结论：历史门禁是否通过

**不通过。**

| 门禁 | 结果 |
| --- | --- |
| 当前树（`ec07409`） | 通过。敏感路径已删，骨架/对标摘要精确关键词无命中。 |
| 分支历史可达性 | 不通过。至少 4 个高风险 blob + 1 个脱敏前骨架 blob + 早期骨架 2 版仍被本分支祖先指向。 |
| 公开仓含义 | 删文件 ≠ 清除对象。任何人打开 `1be59c7` / `6b46b6e` / `e896cef` 仍可读到敏感 blob。 |

未执行：filter-repo、BFG、force-push、合入 `main`、删除远程分支、回写会话/Notion 全文。

## 4. 建议处置（只建议，本扫描不执行）

- **A. 保持 draft，不合入**，直至完成历史清理。现状可继续讨论骨架，但 **不要 merge**。适合先冻结、再等书面授权。
- **B. 新开干净分支，只 cherry-pick / 重放脱敏后的树**（实质是以 `ec07409` 树为根、或从 `main` 重新加入当前两份已脱敏文档）。旧分支保留为归档或转私有，不合入。**推荐**：不必 force-push，公开默认历史上不再挂敏感 blob。
- **C. BFG / `git filter-repo` 后对本分支 force-push**。能从该 ref 去掉敏感 blob，但 **必须用户书面同意**；且 GitHub 缓存、fork、已检出 clone、PR 引用可能仍短期可见，还需按平台流程清缓存。本任务明确禁止擅自执行。

操作顺序建议：先选 A 锁合入 → 再选 B 出干净 PR；仅当必须复用原分支名时再书面授权 C。
